# Changelog

## 0.1.0

- Initial public alpha of Selvedge as a standalone CLI and dashboard package.
- Added generic `selvedge init` behavior for non-GameHub projects.
- Added local `.selvedge/` schemas, workflow planning, dashboard status snapshots,
  stop-condition handling, heartbeat output, and explicit opt-in auto-push.
